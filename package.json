{
  "name": "chase-2020",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "set-domain": "echo \"www.chase.earth\" > dist/_nuxt/static/CNAME",
    "generate-gh": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "nuxt": "^2.14.6",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {}
}
